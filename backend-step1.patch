diff --git a/backend/server.py b/backend/server.py
--- a/backend/server.py2026-04-16 21:38:38.000000000 +0000
+++ b/backend/server.py2026-04-17 00:36:51.000000000 +0000
@@ -142,6 +142,28 @@
     return round(quoted_hours / total_hrs, 2)
 
 
+def get_staffing_status(required_staff: float, allocated_staff_count: int) -> str:
+    if required_staff <= 0:
+        return "neutral"
+    if allocated_staff_count <= 0:
+        return "unassigned"
+    if allocated_staff_count + 0.01 < required_staff:
+        return "under"
+    if allocated_staff_count > required_staff + 0.5:
+        return "over"
+    return "ok"
+
+
+def get_schedule_status(forecast_end_date: Optional[str], target_end_date: Optional[str]) -> str:
+    if not forecast_end_date or not target_end_date:
+        return "neutral"
+    forecast = date.fromisoformat(forecast_end_date)
+    target = date.fromisoformat(target_end_date)
+    if forecast > target:
+        return "late"
+    return "on_track"
+
+
 def get_progress_indicator(quoted_hours: float, logged_hours: float) -> str:
     if quoted_hours <= 0:
         return "neutral"
@@ -190,6 +212,7 @@
     name: str
     description: str = ""
     start_date: str
+    target_end_date: Optional[str] = None
     saturday_enabled: bool = False
     location_region: str = "AUK"
     template_id: Optional[str] = None
@@ -199,6 +222,7 @@
     name: Optional[str] = None
     description: Optional[str] = None
     start_date: Optional[str] = None
+    target_end_date: Optional[str] = None
     saturday_enabled: Optional[bool] = None
     location_region: Optional[str] = None
     status: Optional[str] = None
@@ -209,6 +233,7 @@
     end_date: Optional[str] = None  # if set with start_date, duration calculated
     duration_days: int = 5
     quoted_hours: float = 0
+    allocated_staff_count: int = 0
     order: int = 0
     dependencies: List[str] = []
     assigned_to: List[str] = []
@@ -220,6 +245,7 @@
     end_date: Optional[str] = None
     duration_days: Optional[int] = None
     quoted_hours: Optional[float] = None
+    allocated_staff_count: Optional[int] = None
     order: Optional[int] = None
     dependencies: Optional[List[str]] = None
     assigned_to: Optional[List[str]] = None
@@ -292,8 +318,10 @@
 
     logs = await db.hour_logs.find({"task_id": task["id"]}, {"_id": 0}).to_list(1000)
     logged = sum(entry.get("hours", 0) for entry in logs)
-    req_staff = calculate_required_staff(task["quoted_hours"], start, end, sat, region)
-    indicator = get_progress_indicator(task["quoted_hours"], logged)
+    req_staff = calculate_required_staff(task.get("quoted_hours", 0), start, end, sat, region)
+    allocated_staff_count = int(task.get("allocated_staff_count", 0) or 0)
+    staffing_status = get_staffing_status(req_staff, allocated_staff_count)
+    indicator = get_progress_indicator(task.get("quoted_hours", 0), logged)
 
     return {
         "id": task["id"],
@@ -302,9 +330,12 @@
         "start_date": task["start_date"],
         "end_date": task["end_date"],
         "duration_days": task["duration_days"],
-        "quoted_hours": task["quoted_hours"],
+        "quoted_hours": task.get("quoted_hours", 0),
         "logged_hours": logged,
         "required_staff": req_staff,
+        "allocated_staff_count": allocated_staff_count,
+        "staff_gap": round(req_staff - allocated_staff_count, 2),
+        "staffing_status": staffing_status,
         "order": task["order"],
         "dependencies": task.get("dependencies", []),
         "assigned_to": task.get("assigned_to", []),
@@ -471,6 +502,24 @@
         p["completed_tasks"] = completed
         p["total_quoted_hours"] = total_quoted
         p["total_logged_hours"] = total_logged
+        p["forecast_end_date"] = p.get("end_date")
+        p["schedule_status"] = get_schedule_status(p.get("end_date"), p.get("target_end_date"))
+        p["understaffed_tasks"] = 0
+        for t in tasks:
+            start_date = t.get("start_date")
+            end_date = t.get("end_date")
+            if not start_date or not end_date:
+                continue
+            required_staff = calculate_required_staff(
+                t.get("quoted_hours", 0),
+                date.fromisoformat(start_date),
+                date.fromisoformat(end_date),
+                p.get("saturday_enabled", False),
+                p.get("location_region", "AUK"),
+            )
+            staffing_status = get_staffing_status(required_staff, int(t.get("allocated_staff_count", 0) or 0))
+            if staffing_status in {"under", "unassigned"}:
+                p["understaffed_tasks"] += 1
         p["overall_indicator"] = get_progress_indicator(total_quoted, total_logged)
         result.append(p)
     return result
@@ -486,6 +535,7 @@
         "description": req.description,
         "start_date": req.start_date,
         "end_date": req.start_date,
+        "target_end_date": req.target_end_date,
         "saturday_enabled": req.saturday_enabled,
         "location_region": req.location_region,
         "created_by": req.created_by,
@@ -511,6 +561,7 @@
                     "end_date": req.start_date,
                     "duration_days": tt["duration_days"],
                     "quoted_hours": 0,
+                    "allocated_staff_count": 0,
                     "order": tt["order"],
                     "dependencies": dep_ids,
                     "assigned_to": [],
@@ -536,8 +587,16 @@
     project["tasks"] = task_responses
     total_quoted = sum(t["quoted_hours"] for t in task_responses)
     total_logged = sum(t["logged_hours"] for t in task_responses)
+    total_required_staff = round(sum(t["required_staff"] for t in task_responses), 2)
+    total_allocated_staff = sum(t.get("allocated_staff_count", 0) for t in task_responses)
+    understaffed_tasks = sum(1 for t in task_responses if t.get("staffing_status") in {"under", "unassigned"})
+    project["forecast_end_date"] = project.get("end_date")
+    project["schedule_status"] = get_schedule_status(project.get("end_date"), project.get("target_end_date"))
     project["total_quoted_hours"] = total_quoted
     project["total_logged_hours"] = total_logged
+    project["total_required_staff"] = total_required_staff
+    project["total_allocated_staff"] = total_allocated_staff
+    project["understaffed_tasks"] = understaffed_tasks
     project["overall_indicator"] = get_progress_indicator(total_quoted, total_logged)
     return project
 
@@ -588,6 +647,7 @@
         "start_date_override": req.start_date,  # None if auto, date string if manual
         "duration_days": req.duration_days,
         "quoted_hours": req.quoted_hours,
+        "allocated_staff_count": req.allocated_staff_count,
         "order": req.order,
         "dependencies": req.dependencies,
         "assigned_to": req.assigned_to,
@@ -752,6 +812,12 @@
         "tasks": task_summaries,
         "total_quoted_hours": total_quoted,
         "total_logged_hours": total_logged,
+        "total_required_staff": round(sum(t["required_staff"] for t in task_summaries), 2),
+        "total_allocated_staff": sum(t.get("allocated_staff_count", 0) for t in task_summaries),
+        "understaffed_tasks": sum(1 for t in task_summaries if t.get("staffing_status") in {"under", "unassigned"}),
+        "forecast_end_date": project.get("end_date"),
+        "target_end_date": project.get("target_end_date"),
+        "schedule_status": get_schedule_status(project.get("end_date"), project.get("target_end_date")),
         "overall_indicator": get_progress_indicator(total_quoted, total_logged),
         "total_tasks": len(tasks),
         "completed_tasks": sum(1 for t in task_summaries if t["status"] == "completed"),
